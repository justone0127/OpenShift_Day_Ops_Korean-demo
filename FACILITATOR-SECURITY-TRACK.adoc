= 보안 트랙 진행자 가이드 (RHACS · ZTWIM · Secrets)
:toc: left
:toclevels: 2

나루페이 침해사고 시나리오로 묶인 세 모듈(RHACS → ZTWIM → Secrets Management)의
진행자용 문서입니다. 참가자에게 공개하지 마십시오 — 퀴즈 정답이 포함되어 있습니다.

== 트랙 구성

[cols="1,2,1,2",options="header"]
|===
| 페이지 | 내용 | 시간 | 참가자가 실행하는 명령

| 인트로 — `15-00-security-story.adoc`
| 침해사고 스토리(다이어그램·타임라인·피해 집계) + 워밍업 퀴즈 3문항
| 10분
| 없음 — 읽기 전용

| 모듈 1 — `15-acs.adoc`
| RHACS 콘솔에서 취약/안전 워크로드 비교 → 정책 차단 시연 + 퀴즈 3문항
| 15분
| 6블록 (콘솔 클릭이 대부분)

| 모듈 2 — `17-ztwim.adoc`
| SVID 확인 + 비밀번호 없는 연결 + 무권한 접속 거부 + 퀴즈 2문항
| 10분
| 5블록

| 모듈 3 — `16-secrets-management.adoc`
| Secret의 실제 보호 수준 확인 + Vault 개념 + 퀴즈 2문항 + *보안 트랙 마무리*
| 10분
| 3블록

| *합계*
|
| *45분*
| *실행 블록 14개*
|===

[IMPORTANT]
.진행 순서에 주의하십시오
====
화면 순서는 *RHACS → ZTWIM → Secrets Management* 입니다. 파일명 순서(15 → 16 → 17)와 다릅니다.

파일명의 `15/16/17` 은 전체 로드쇼(01~14번 모듈)와 번호가 겹치지 않게 유지한 것이고,
실제 진행 순서와 표시 번호는 `nav.adoc` 이 결정합니다.
참가자 화면에는 *모듈 1 / 2 / 3* 으로만 표시됩니다.

스토리상으로는 공격 2단계(비밀번호)보다 3단계(네트워크 신뢰)를 먼저 다루게 됩니다.
"신원으로 대체할 수 있는 것은 대체하고, **그래도 남는 비밀**을 마지막에 다룬다"는 흐름이며,
모듈 2 끝과 모듈 3 퀴즈 2 해설에서 이 연결을 명시적으로 짚어 줍니다.
====

설계 원칙은 **"참가자는 설치하지 않는다"** 입니다.
긴 YAML 붙여넣기는 물론이고 **환경 배포·정리 스크립트도 참가자 가이드에서 전부 제외**했습니다.
참가자는 관찰과 판단에만 시간을 쓰고, 환경 준비와 정리는 온전히 진행자의 몫입니다.

[IMPORTANT]
====
참가자 가이드에는 배포 스크립트를 실행하는 절차가 **없습니다.**
워크로드가 없으면 참가자에게는 "진행자에게 알려 주십시오"라고만 안내됩니다.
따라서 아래 사전 준비는 반드시 진행자가 완료해 두셔야 합니다.
====

=== 퀴즈 형식

퀴즈는 라디오 버튼으로 답을 고르고 *정답 확인* 버튼을 누르면 즉시 채점됩니다.
정답은 초록, 오답은 빨강으로 표시되고 해설이 그때 펼쳐집니다. 다른 보기를 고르면 다시 풀 수 있습니다.

* 구현: `content/supplemental-ui/js/nrp-quiz.js` + `css/site-extra.css`
* 정답은 각 퀴즈 블록의 `data-answer` 속성에 있습니다 — **참가자 브라우저에서 소스를 보면 확인 가능합니다.**
  채점 편의를 위한 학습용 장치이며, 평가용으로는 적합하지 않습니다.
* JS가 로드되지 않으면 해설이 처음부터 펼쳐진 채로 보입니다(내용 손실 없음).


== 사전 준비 (진행자)

=== 필수 조건

* OpenShift 4.14+ 클러스터, `cluster-admin` 권한
* bastion에 `oc`, `git`, `curl` (Helm은 스크립트가 자동 설치)

실습 환경에 무엇이 이미 있고 무엇이 없는지 정리하면 다음과 같습니다.

[cols="1,1,3",options="header"]
|===
| 구성 요소 | 실습 환경 | 담당자가 할 일

| *RHACS* (Central + SecuredCluster)
| 사전 설치됨
| 워크로드 배포만 (배포 스크립트가 수행)

| *ZTWIM 오퍼레이터*
| 사전 설치됨
| SPIRE 구성·실습 워크로드 배포 (배포 스크립트가 수행)

| *HashiCorp Vault*
| **포함되어 있지 않음**
| **직접 설치·구성해야 합니다** (배포 스크립트에 포함)
|===

[IMPORTANT]
====
Vault 는 실습 환경에 포함되어 있지 않습니다. 설치 형상, 점검 스크립트, 트러블슈팅은
별도 문서로 정리했습니다 — link:FACILITATOR-VAULT-SETUP.adoc[FACILITATOR-VAULT-SETUP.adoc]

준비하지 않으면 모듈 3 의 Vault UI 실습에서 막힙니다.
====

=== 실행

참가자가 받는 **각 클러스터에서 한 번씩** 실행합니다.

[source,bash]
----
cd ~/
git clone -b ocp-days-ops-1.0.0 https://github.com/justone0127/OpenShift_Day_Ops_Korean-demo.git
cd OpenShift_Day_Ops_Korean-demo/setup-scripts/security-track-scripts
./setup-security-track.sh all
----

[cols="2,3",options="header"]
|===
| 환경 구성 | 명령

| **참가자마다 클러스터를 하나씩** (기본)
| `./setup-security-track.sh all` — 각 클러스터에서 한 번씩

| 클러스터 하나를 여러 명이 공유
| `./setup-multiuser.sh deploy 30` — 사용자별 네임스페이스 분리
|===

기본 구성은 **참가자마다 전용 클러스터를 admin 계정으로 받는 방식**입니다.
이 경우 네임스페이스는 `narupay` / `postgresql-spiffe` / `postgresql-spiffe-client` 이고,
참가자는 이미 관리자로 접속되어 있어 별도 로그인이 필요 없습니다.

[NOTE]
====
`setup-security-track.sh` 는 `setup-multiuser.sh` 의 **단일 환경 모드**를 부르는 얇은 래퍼입니다.
로직이 한 곳에 있어 두 경로가 어긋나지 않습니다.

클러스터를 공유하는 구성이 필요해지면 `setup-multiuser.sh deploy N` 을 쓰십시오.
이 경우에만 네임스페이스에 `user1-` 같은 접두사가 붙습니다.
====

수행 순서입니다:

. RHACS admission controller 활성화 (모듈 1 차단 시연에 필요)
. SPIRE 플랫폼 구성 — 이미 있으면 건너뜁니다
. Vault 설치·구성 — 이미 있으면 건너뜁니다
. `narupay` / `postgresql-spiffe` / `postgresql-spiffe-client` 네임스페이스와 워크로드 배포
. Vault 에 `secret/narupay/payment-db` 시드

약 8~12분 소요됩니다. 대부분 Vault Helm 설치와 SPIRE 구성 대기 시간입니다.

[IMPORTANT]
====
**워크샵 시작 전에 반드시 완료해 두십시오.** 참가자 가이드에는 이 절차가 없으므로,
준비되지 않은 상태로 세션을 시작하면 모듈 1 첫 단계에서 바로 막힙니다.

RHDP/AgnosticV 환경이라면 이 스크립트를 프로비저닝 단계에 포함시키는 것이 가장 좋습니다.
====

=== 확인

[source,bash]
----
./setup-security-track.sh status
./verify-vault.sh              # Vault 상세 점검 (실패 시 종료 코드 1)
----

[source,text]
----
  [✓] 단일 사용자    narupay(1/1/1)  spiffe(1/1)  vault(✓)
----

`✓` 가 나올 때까지 몇 번 다시 실행해 보십시오. SPIRE SVID 발급에 1~2분 걸릴 수 있습니다.

NOTE: `openshift-zero-trust-workload-identity-manager` namespace 에
`spire-spiffe-oidc-discovery-provider` 파드가 **없는 것이 정상**입니다.
이 실습에서 쓰지 않아 배포하지 않습니다 (아래 _진행 시 주의점 → 모듈 2_ 참고).

=== 다중 사용자 배포 (참가자마다 계정을 주는 경우)

참가자마다 계정(`user1`, `user2`, ...)이 하나씩 주어지는 환경에서는
`setup-multiuser.sh` 로 사용자별 격리 환경을 만듭니다.

[source,bash]
----
./setup-multiuser.sh deploy 30    # user1~user30 + admin
./setup-multiuser.sh status 30
./setup-multiuser.sh cleanup 30
----

[TIP]
.진행자 확인용 환경
====
번호 사용자 외에 **`admin` 계정 환경도 기본으로 함께 만듭니다**(`admin-narupay` 등).
참가자와 똑같은 화면으로 미리 훑어보실 수 있습니다.

[source,bash]
----
./setup-multiuser.sh deploy 0                    # admin 것만 (확인용)
EXTRA_USERS="" ./setup-multiuser.sh deploy 30    # 참가자 것만
EXTRA_USERS="admin lab-user" ./setup-multiuser.sh deploy 30
----

이 구성에서는 네임스페이스에 접두사가 붙으므로, 가이드의 네임스페이스 표기도 함께 조정해야 합니다.
====

사용자당 만들어지는 것:

[cols="1,2",options="header"]
|===
| 리소스 | 내용

| `userN-narupay`
| `payment-api`, `payment-api-secure`, `legacy-secret-app` +
  차단 시연용 매니페스트 ConfigMap(`narupay-lab-manifests`)

| `userN-postgresql-spiffe`
| PostgreSQL 서버 + 전용 `ClusterSPIFFEID`

| `userN-postgresql-spiffe-client`
| 클라이언트 + 전용 `ClusterSPIFFEID`

| RoleBinding
| `userN` 에게 위 세 네임스페이스 `admin`

| Vault
| `secret/userN-narupay/payment-db`
|===

참가자에게 보이는 프로젝트는 **자기 것 세 개뿐**입니다. 다른 참가자의 네임스페이스나
`stackrox` / `vault` / SPIRE 네임스페이스는 목록에 나오지 않습니다.

다만 가이드에 공유 자원을 조회하는 명령이 세 개 있어, 그것만 읽을 수 있는
최소 권한을 `system:authenticated` 그룹에 부여합니다(사용자 수와 무관하게 1회):

[cols="2,2,2",options="header"]
|===
| 명령 | 필요한 권한 | 용도

| `oc get route central -n stackrox`
| `stackrox` 의 routes get/list
| 모듈 1 — RHACS 콘솔 주소

| `oc get securedcluster -n stackrox`
| `stackrox` 의 securedclusters get/list
| 모듈 1 — admission controller 확인

| `oc get route vault -n vault`
| `vault` 의 routes get/list
| 모듈 3 — Vault UI 주소
|===

NOTE: 이 권한이 없으면 해당 명령에서 `Forbidden` 이 납니다.
`setup-multiuser.sh deploy` 가 자동으로 부여하므로 따로 하실 일은 없습니다.

계정 자체는 만들지 않습니다. 실습 환경이 `user1`~`userN` 을 제공한다고 가정하고
권한만 부여합니다. 접두사는 `USER_PREFIX`, 시작 번호는 `USER_START`,
추가 계정은 `EXTRA_USERS` 로 바꿀 수 있습니다.

[IMPORTANT]
.이 경우에만 가이드를 손봐야 합니다
====
기본 가이드는 `narupay` 처럼 접두사 없는 네임스페이스를 전제로 쓰여 있습니다.
클러스터를 공유하는 구성으로 진행하시려면 참가자에게 본인 접두사를 안내하거나,
가이드의 네임스페이스 표기를 해당 방식에 맞게 조정해야 합니다.
====

[NOTE]
.공유되는 것
====
사용자별로 나누지 않는 자원입니다.

* *RHACS 정책* — 클러스터 전역입니다. 먼저 도달한 참가자가 `Latest tag` 를
  `Inform and enforce` 로 바꾸면 이후 참가자에게도 이미 켜져 있습니다.
  가이드에 이 경우 "확인만 하고 넘어가라"고 안내해 두었습니다. 차단 결과는 동일합니다.
* *SPIRE 플랫폼* — `SpireServer` 등은 클러스터에 하나입니다. 사용자별로는
  `ClusterSPIFFEID` 와 워크로드만 나뉩니다.
* *Vault 인스턴스* — 경로만 사용자별로 나눕니다. dev mode 라 root 토큰으로
  다른 사용자 경로도 볼 수 있습니다. 학습 환경 기준으로 허용한 부분입니다.
====

=== 정리

[cols="1,3",options="header"]
|===
| 명령 | 지우는 것

| `./setup-multiuser.sh cleanup 30`
| 사용자별 네임스페이스·`ClusterSPIFFEID`·Vault 경로. +
  공유 플랫폼(SPIRE, Vault)은 유지 — 같은 환경에서 다시 배포할 때 빠릅니다.

| `./setup-multiuser.sh cleanup-all 30`
| 위에 더해 SPIRE 커스텀 리소스와 **datastore PVC**, Vault 까지 제거. +
  처음부터 다시 구성할 때 씁니다.
|===

[IMPORTANT]
====
`cleanup-all` 이 SPIRE datastore PVC 를 함께 지우는 것이 핵심입니다.
등록 엔트리와 CA 가 sqlite 에 남아 있으면 "새로 구성"해도 이전 상태가 그대로 살아납니다.

**ZTWIM 오퍼레이터와 그 namespace 는 지우지 않습니다.** 실습 환경이 사전 설치해
제공하는 요소이므로, 삭제하면 우리가 만들지 않은 인프라까지 파괴됩니다.
====

NOTE: RHACS 정책의 Enforcement 설정은 스크립트가 되돌리지 않습니다.
콘솔에서 *Policy Management* → *Latest tag* → *Edit policy* → *Policy behavior* → *Actions* →
*Enforcement* 를 `Inform` 으로 되돌리십시오.

== 진행 시 주의점

=== 모듈 1 — RHACS

* **위반 사항 표시 지연** — RHACS가 워크로드를 수집하고 평가하는 데 30초~1분이 걸립니다.
  참가자가 "안 보여요"라고 하면 먼저 새로고침을 안내하십시오.
* **정책 이름은 RHACS 버전마다 조금씩 다릅니다.** 가이드에도 명시했지만,
  `Latest tag` 는 검색창에 `latest` 로 찾도록 안내하는 것이 안전합니다.
* **Vulnerability Management 메뉴 구조도 버전마다 다릅니다.**
+
--
[cols="1,2",options="header"]
|===
| 버전 | 경로

| 최신
| *Vulnerability Management* → *Results* → 상단 *User Workloads* 탭
  → 보기 전환에서 *Deployments*

| 이전
| *Vulnerability Management* → *Workloads*
|===

가이드에는 두 경우를 모두 적어 두었습니다. 도착하는 화면은 같습니다.

**기본 필터 3개(`Critical`, `Important`, `Fixable`)가 걸린 상태로 열립니다.**
참가자가 "숫자가 왜 이렇게 적죠?"라고 물으면 *Clear filters* 를 눌러 보게 하십시오.
이 기본값 자체가 "전부 나열하지 않고 먼저 손댈 것부터 보여 준다"는 RHACS 의 관점이라,
설명 소재로 쓰기 좋습니다.
--
* **`payment-api` 가 Vulnerability Management 목록에 없을 수 있습니다. 정상입니다.**
+
--
`ubi9/ubi:latest` 는 Red Hat 이 자주 재빌드하므로 오늘자 이미지에는 CVE 가 잡히지 않을 수 있고,
그 화면은 취약점이 발견된 것만 나열합니다. 고정 태그인 `payment-api-secure`(`9.4`)에는
그 시점 이후 CVE 가 쌓여 있어 이쪽만 보입니다.

참가자가 "취약 버전이 왜 없죠?"라고 물으면 한 문장으로 정리해 주십시오 —
**"CVE 가 없다고 안전한 게 아닙니다. 저 워크로드의 진짜 문제는 1단계 Violations 에서 이미 봤습니다."**

수치를 맞추려고 이미지 태그를 바꾸지 마십시오. `latest` 여야 3~4단계의 차단 시연이 성립합니다.
--
* **확실한 대비는 1단계의 *Violations* 입니다.** 배포 방식의 문제라 이미지가 바뀌어도
  결과가 흔들리지 않습니다.
* **정책이 Disabled 상태일 수 있습니다.** Enforce로 바꾸기 전에 Enable부터 확인시키십시오.
* **범위 제한을 빠뜨리면 모듈 2 가 깨집니다. 가장 중요한 주의점입니다.**
+
--
`Latest tag` 정책은 클러스터 전역입니다. 범위를 제한하지 않고 `Enforce on Deploy` 를 켜면
`latest` 태그를 쓰는 다른 워크로드까지 차단됩니다.

**모듈 2 의 PostgreSQL SPIFFE 워크로드가 `latest` 태그를 씁니다:**

[source,text]
----
quay.io/ablock/spiffe-helper-postgresql:latest
registry.redhat.io/rhel9/postgresql-16:latest
----

범위를 제한하지 않으면 이 파드들이 0개로 내려가고, 모듈 2 에서
`No resources found` 만 보게 됩니다. Deployment 는 남아 있어서 배포 스크립트도
"already deployed" 로 판단해 문제를 못 잡습니다.

가이드는 `Actions` 보다 **먼저 `Scope` 에서 namespace 를 `narupay` 로 제한**하도록
순서를 잡아 두었습니다. 참가자가 이 단계를 건너뛰지 않도록 확인해 주십시오.

이미 발생한 경우 복구 방법:

[source,bash]
----
# 1. 정책 범위를 narupay 로 제한하거나 Enforcement 를 Inform 으로 되돌린 뒤
# 2. 워크로드를 다시 올립니다
oc scale deployment postgresql-spiffe -n postgresql-spiffe --replicas=1
oc scale deployment postgresql-spiffe-client -n postgresql-spiffe-client --replicas=1
----
--
* **차단 시연이 실패하는 경우** — 대부분 admission controller 문제입니다:
+
[source,bash]
----
oc get securedcluster -n stackrox -o jsonpath='{.items[0].spec.admissionControl}{"\n"}'
----
+
`listenOnCreates: true` 가 없으면 배포 스크립트를 다시 실행하십시오.
* **`privileged: true` 는 매니페스트에서 의도적으로 뺐습니다.** OpenShift의 기본 SCC(`restricted-v2`)가
  이를 먼저 차단해서 pod가 아예 뜨지 않기 때문입니다. 질문이 나오면
  "OpenShift SCC가 이미 한 겹 막아 주고 있고, RHACS는 그 위의 계층"이라고 설명하시면 좋습니다.

=== 모듈 2 — ZTWIM

* SPIRE 구성이 가장 무겁습니다. **반드시 사전에 배포**해 두십시오.
* **OIDC Discovery Provider 는 일부러 배포하지 않습니다.**
+
--
`SpireOIDCDiscoveryProvider` 는 JWT-SVID 를 OIDC 페더레이션(예: AWS IAM)에 연결하기 위한
컴포넌트로, 이 실습(X509-SVID + PostgreSQL mTLS)에서는 사용하지 않습니다.

배포하면 자신의 등록 엔트리가 에이전트에 전파되기 전까지 다음 로그를 반복하고
그동안 파드가 Ready 가 되지 않습니다:

[source,text]
----
level=error msg="No identity issued" method=FetchJWTBundles registered=false
level=warning msg="Failed to fetch JWKS from the Workload API" error="... no identity issued"
----

쓰지도 않는 컴포넌트 때문에 "파드 하나가 안 뜬다"는 질문을 받게 되므로 제외했습니다.
`oc get pods -n openshift-zero-trust-workload-identity-manager` 에
`spire-spiffe-oidc-discovery-provider` 가 **보이지 않는 것이 정상**입니다.

JWT-SVID/OIDC 확장 시나리오를 진행하려면 켤 수 있습니다:

[source,bash]
----
ZTWIM_ENABLE_OIDC_PROVIDER=true ./configure-ztwim-lab.sh
----
--
* `attacker` pod 실습에서 이미지 pull에 시간이 걸릴 수 있습니다(`registry.redhat.io` 인증 필요).
  실패하면 `oc get pod attacker -n postgresql-spiffe-client -o yaml | grep -A5 status` 로 확인하십시오.
* 이 모듈의 감정적 정점은 **`attacker` pod가 거부되는 순간**입니다.
  그 직전에 "이 pod는 같은 namespace에 있고, 포트도 열려 있고, NetworkPolicy도 안 막습니다"를
  한 번 짚어 주면 임팩트가 커집니다.

=== 모듈 3 — Secrets Management

* 이 모듈의 하이라이트는 **두 번째 실습**(`oc exec ... env`)입니다.
  "RBAC을 아무리 걸어도 이 경로는 안 지나간다"는 지점에서 한 번 멈추고 반응을 확인하십시오.
* Vault는 **dev mode**입니다. pod가 재시작되면 데이터가 사라집니다.
  세션 중 Vault UI가 비어 있으면 `./verify-vault.sh --fix` 로 다시 시드하십시오.

== 퀴즈 정답 및 해설 요약

가이드에서는 참가자가 답을 고른 뒤 채점될 때 해설이 펼쳐집니다.
아래는 진행자가 미리 훑어 둘 요약본입니다.

=== 인트로 (15-00) — 워밍업 3문항

[cols="1,1,4",options="header"]
|===
| 문항 | 정답 | 진행자가 강조할 지점

| Q0-1
| *(B)*
| CI 스캔은 시점의 스냅샷. 클러스터는 계속 살아 있고 CVE는 매일 새로 나온다.

| Q0-2
| *(C)*
| Base64는 인코딩이지 암호화가 아니다. etcd 암호화는 기본값이 아닌 별도 옵션.

| Q0-3
| *(B)*
| M&M 보안 — 한 번만 뚫리면 되는 구조. 횡적 이동을 막을 계층이 없다.
|===

=== 모듈 1 — RHACS (3문항)

[cols="1,1,4",options="header"]
|===
| 문항 | 정답 | 진행자가 강조할 지점

| 퀴즈 1
| *(B)*
| 두 가지가 겹쳐 있다 — (1) 시간이 흘러 새 CVE가 나왔고 (2) 배포 방식의 문제는 이미지 스캐너가 볼 수 없는 영역이다.

| 퀴즈 2
| *(B)*
| Fixable = 패치가 이미 존재. 투입 대비 효과가 가장 높은 작업을 골라내는 필터.
  Fixable이 아닌 것도 여전히 위험하며 완화 조치로 대응한다는 점을 덧붙이면 좋습니다.

| 퀴즈 3
| *(C)*
| 사람의 협조 → 플랫폼의 강제. **(D)는 흔한 오답** — Deploy enforcement는 새 요청만 막고
  기존 워크로드를 소급 제거하지 않습니다. 실습에서 `oc delete` 를 먼저 한 이유입니다.
|===

=== 모듈 2 — ZTWIM (2문항)

[cols="1,1,4",options="header"]
|===
| 문항 | 정답 | 진행자가 강조할 지점

| 퀴즈 1
| *(B)*
| 짧은 수명의 두 효과 — 악용 창 축소 + 폐기(CRL/OCSP) 인프라 회피.
  "폐기가 그냥 기다리기가 된다"는 표현이 잘 먹힙니다. 자동 갱신이 전제 조건.

| 퀴즈 2
| *(B)*
| label은 **자기 신고**라 위조 가능. SVID는 SPIRE가 노드에서 검증 후 발급.
  NetworkPolicy와 SPIFFE는 대체가 아니라 **계층**이라는 결론으로 마무리하십시오.
|===

=== 모듈 3 — Secrets Management (2문항)

[cols="1,1,4",options="header"]
|===
| 문항 | 정답 | 진행자가 강조할 지점

| 퀴즈 1
| *(B)*
| RBAC은 API 서버 앞의 검문소. kubelet이 이미 값을 주입한 뒤의 컨테이너 내부는 API를 거치지 않는다.
  (D) 관련해 볼륨 마운트가 환경변수보다 나은 점(값 갱신 시 파일이 자동 반영)을 짚어 주면 좋습니다.

| 퀴즈 2
| *(B)*
| **트랙에서 가장 중요한 개념적 지점입니다.** "Vault 도입" ≠ "secret 관리 잘함".
  정적 값을 그냥 옮기면 감사/정책은 얻지만 수명 문제는 그대로다.
  Vault의 진짜 가치는 동적 자격 증명(요청 시 생성, TTL 후 삭제)에 있다.
|===

== 놓치면 안 되는 장면

트랙 전체의 임팩트는 이 두 순간에서 나옵니다. 진행 속도를 조절하실 때 기준으로 삼으십시오.

. **모듈 1의 3~4단계** — 탐지에서 차단으로 전환한 뒤, 취약한 배포가 **실제로 거부되는** 순간
. **모듈 2의 `attacker` pod 거부** — 같은 namespace, 열린 포트, NetworkPolicy 없음.
  그런데도 거부됩니다

== 관련 파일

[cols="2,3",options="header"]
|===
| 경로 | 용도

| `setup-scripts/security-track-scripts/setup-multiuser.sh`
| 환경 배포 / 상태 확인 / 정리 (다중·단일 공통 로직)

| `setup-scripts/security-track-scripts/setup-security-track.sh`
| 단일 사용자용 진입점 (위 스크립트의 단일 사용자 모드 래퍼)

| `setup-scripts/security-track-scripts/verify-vault.sh`
| 모듈 3 Vault 상세 점검

| `setup-scripts/security-track-scripts/manifests/payment-api-vulnerable.yaml`
| 모듈 1 — 취약 버전 (차단 시연에 재사용)

| `setup-scripts/security-track-scripts/manifests/payment-api-secure.yaml`
| 모듈 1 — 강화 버전

| `setup-scripts/security-track-scripts/manifests/legacy-secret-app.yaml`
| 모듈 3 — Secret 주입 방식 애플리케이션

| `setup-scripts/vault-config-scripts/`
| 모듈 3 — Vault 설치·구성 (기존 스크립트, 오케스트레이터가 호출)

| `setup-scripts/ztwim-config-scripts/`
| 모듈 2 — SPIRE 플랫폼·PostgreSQL 실습 (기존 스크립트, 오케스트레이터가 호출)

| `content/antora.yml`
| `workshop_repo_url` / `workshop_repo_dir` 속성. 참가자 가이드에서 설치 절차를 제거하면서
  현재는 사용되지 않습니다. 포크한 경우 위 `git clone` 주소만 포크 주소로 바꾸십시오.
|===
